# Redirect http://<host>/ to https://<host>:1000/ (plain HTTP must not hit the old rag-system proxy on :8000)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 301 https://$host:1000$request_uri;
}
